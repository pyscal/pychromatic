# Development dependencies are now managed in pyproject.toml [project.optional-dependencies.dev]
# Install with: pip install -e ".[dev]"
#
# This file is kept for backward compatibility with tools that expect it.
-e ".[dev]"
